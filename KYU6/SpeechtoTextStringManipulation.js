const bot = {
    //const weatherData=fetch().then(response=>response.text())
    message(prompt) {
      let result=0;
      if(prompt.includes('Add')){
        result=prompt.replace('Add','').trim().m
      }
      else if(prompt.includes('Substract')){
        
      }
        return ;
    }
};